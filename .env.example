NEXT_PUBLIC_API_URL=http://localhost:8001
# Optional: only set this to bypass the backend's /playstat proxy and hit
# playstat directly (e.g. local playstat dev without the backend running).
# NEXT_PUBLIC_PLAYSTAT_API_URL=http://localhost:8000
# Must match a key in the backend's BUDGERR_API_KEYS.
NEXT_PUBLIC_BUDGERR_API_KEY=
